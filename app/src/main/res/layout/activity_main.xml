<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
            name="vm"
            type="com.moneyweather.viewmodel.MainViewModel" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/white"
        tools:context="com.moneyweather.view.MainActivity">


        <androidx.viewpager2.widget.ViewPager2
            android:id="@+id/vpPage"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            app:layout_constraintBottom_toTopOf="@id/viewLine"
            app:layout_constraintTop_toTopOf="parent"
            />

        <View
            android:id="@+id/viewLine"
            style="@style/HorizontalLine"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toTopOf="@id/tabBottom"
            />

        <com.google.android.material.tabs.TabLayout
            android:id="@+id/tabBottom"
            android:layout_width="match_parent"
            android:layout_height="@dimen/action_bar_height"
            android:background="#00ffffff"
            app:tabTextAppearance="@style/bottom_tab_text"
            app:tabRippleColor="@color/transparent"
            app:tabTextColor="@color/gnb_off_color"
            app:tabIndicatorHeight="0dp"
            app:tabSelectedTextColor="@color/default_main_color"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            >
        </com.google.android.material.tabs.TabLayout>

    </androidx.constraintlayout.widget.ConstraintLayout>


</layout>
